syntax = "proto3";
option go_package = "./user";

package template;

message  User{
  string Username = 1;
  string Password = 2;
}

message  LoginResponse{
  string Token = 1;
}

service UserService{
  rpc Login(User) returns (LoginResponse);
}